# Adversarial Review: Institutional Conservation Vertical Slice

Review the current uncommitted code in:

- the sibling `institution` repository
- the sibling `conservation` repository

Focus on `conservation-institution` and its `vertical_slice` tests after the earlier repairs. Do not edit files.

The acceptance claim is precise: one domain-neutral exact incidence model and its derived balance law are interpreted institutionally, then renamed through two valid signature morphisms into ecological trophic-pool vocabulary and economic ledger-account vocabulary. A conserving ecological target trace must make both sides of its satisfaction square true; a violating economic target trace must make both sides false; the supplied cases must therefore exercise both truth values without structural errors.

Adversarially answer:

1. Do the tests actually begin from one shared neutral signature/law, or are they two unrelated source models with merely analogous shapes?
2. Are quantity kinds translated honestly? Is requiring kind identity correct, or does cross-domain renaming require an explicit kind-symbol map?
3. Can valid public constructors create a morphism/model/sentence that triggers internal `expect` panics?
4. Are sentence translation and model reduct genuinely covariant/contravariant and membership preserving?
5. Does false/false represent semantic violation on both sides rather than malformedness?
6. Does the non-vacuity observation use the actual two squares meaningfully?
7. Are the local path-plus-version dependency and package metadata acceptable for local review while correctly blocking public publication until dependency release/pinning?
8. Which exact tests or API changes are blocking before this vertical slice can be called complete?

Return verdict, findings with file/line references, required repairs, and whether final verification may begin. Do not write files.
